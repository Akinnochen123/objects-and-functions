a.	The code returns ‘Tim’ because the variable was defined inside the function displayFirstName that is being invoked.
b.	The code returns a reference error firstName not defined because console.log can’t access the firstName as it is a local scope that can only be accessed by invoking its function.
c.	The code returns ‘Elie’ because the variable firstName is declared as a global scope and can be accessed from anywhere.
d.	The code returns reference error because firstName is not declared as a variable.
e.	The code returns “Hi undefined”.
f.	The code returns “Hi Elie”.
g.	The code returns “Hi”.
h.	The code returns “Hi”.
